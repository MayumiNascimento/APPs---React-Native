import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  
  titulo:{
    fontSize: 24,
    color: 'orange',
    textAlign: 'center',
    marginTop: 20
  },

  contador:{
    fontSize: 60,
    color: 'purple',
    textAlign: 'center',
    marginTop: 20,
    marginBottom: 20,
  },

  separador:{
    marginTop: 10
  }

})

export {styles}