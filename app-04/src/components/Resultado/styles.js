import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginTop: 20,
    padding: 16,
    backgroundColor: '#f0f0f0',
    borderRadius: 8,
  },
  text: {
    fontSize: 18,
    marginBottom: 8,
  },
  message: {
    fontSize: 20,
    fontWeight: 'bold',
    color: '#333',
  },
});