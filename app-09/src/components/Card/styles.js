import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  card: {
    backgroundColor: '#fff',
    borderWidth: 1,
    borderColor: '#ccc',
    padding: 16,
    marginBottom: 16,
    borderRadius: 8,
  },
  titulo: {
    fontSize: 16,
    color: '#1d4ed8',
    fontWeight: 'bold',
    marginBottom: 8,
  },
  texto: {
    fontSize: 14,
    marginBottom: 4,
  },
});
