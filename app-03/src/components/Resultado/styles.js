import {StyleSheet} from 'react-native';
 
const styles = StyleSheet.create({
  container: {

    padding: 20,
    borderRadius: 8,
    alignItems: 'center',
  },
  texto: {
    color: 'green',
    fontSize: 18,
    fontWeight: 'bold',
  },
});
export {styles}