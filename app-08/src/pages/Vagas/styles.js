import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    padding: 25,
  },
  titulo: {
    fontSize: 24,
    color: '#d00000',
    fontWeight: 'bold',
    textAlign: 'center',
    marginBottom: 20,
  },
});
